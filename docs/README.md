# Docs

This repository has one documentation space:

- `docs/memories/` - Internal memories and work notes (historical docs and planning).

User-facing dev setup lives in:

- `README.md`

CLI usage is self-documented via:

- `uv run ms --help`
