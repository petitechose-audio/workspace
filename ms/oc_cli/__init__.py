"""Open Control-style CLI tools (Python).

These are small, standalone commands (oc-build, oc-upload, oc-monitor)
implemented in Python so they can be executed via `uv run` on all platforms.
"""

from __future__ import annotations
